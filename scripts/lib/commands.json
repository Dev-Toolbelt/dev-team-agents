{
  "$schema": "internal://commands.json",
  "_comment": "Per-command metadata. The command body lives at commands/<name>.md. The renderer uses this file to (a) choose the lead agent for opencode/codex, (b) resolve the model id via tiers.json, (c) emit the provider-specific wrapper metadata it can actually enforce, and (d) apply plan gate softening per provider. Add a new row when adding a command.",
  "_codex_prompt_note": "Codex now uses `.codex/skills/devteam-*/SKILL.md` as the official command surface. Runtime model and reasoning settings still live on the spawned custom agents in `.codex/agents/*.toml`; the command skill itself remains orchestration only.",
  "_claude_model_pin": "commands/<name>.md MAY carry a YAML frontmatter block, and Claude Code reads it — that is the only way a command's tier reaches Claude, which symlinks the body and never passes it through the renderer. The renderer strips the block before emitting the opencode template and the Codex prompt, so it is Claude-only. The rule, enforced by helpers/agent-lint.sh: `model:` may appear ONLY on a `repetitive` command, and when present must equal tiers.json.repetitive.claude. This is the same doctrine tiers.json `_claude_note` applies to `effort:` — the key OVERRIDES the session's model, so pinning a `reasoning` command to opus would silently undo a user who lowered the session for cost. A haiku pin is the only one that can never raise it. Presence is permitted, not required: `explain` is `repetitive` and deliberately carries NO pin, because its output is a teaching explanation contextualized in the user's own code and the session model is the one the user chose for that.",
  "_tier_rule": "A command's `tier` MUST equal the tier of the agent named in its `agent` field — helpers/agent-lint.sh fails on any divergence. The two are not independent knobs: on opencode the snippet's `agent` makes the command run AS that agent while `model` comes from the command's tier, so a mismatch runs an agent on a model that is not its own. Two rows had drifted before the rule was written down (`tester` sat in `repetitive` while backend-test-specialist is `backend-exec`; `health-check` sat in `backend-exec` while it named `setup-assistant`, a `reasoning` agent).",
  "_filler_agent_note": "`update`, `symlinks`, `health-check`, `install`, `explain` and `push` spawn NO agent. The first four are thin script/skill runners; `explain` answers in the main context on purpose, because the terms it explains come from the live session and a subagent would receive only the prompt; `push` is a thin wrapper around the `github-actions` skill's own quiz/watch/fix-loop logic and needs no delegate. Their `agent` field is filler the renderer still requires, and all six name `technical-writer` so the filler stays consistent and the tier rule above resolves to `repetitive`, which is what that class of work actually is. Do not read those six rows as a delegation target.",
  "_plan_gate_values": {
    "required": "Always present plan + wait for approval. Used for architectural/planning commands.",
    "conditional": "Use plan mode for complex tasks; execute directly for simple ones. Plan gate text is softened for non-Claude providers.",
    "opt_out": "No plan gate. Used for thin runner commands (update, symlinks)."
  },
  "_interaction_mode_values": {
    "required": "Structured choices are a hard part of the command UX. Codex should keep the interactive chooser requirement and route the user to /plan when unavailable.",
    "optional": "Structured choices are preferred, but the command must still work in normal Codex mode by asking the same question directly in conversation when the chooser is unavailable.",
    "none": "The command should not rely on structured choices at all."
  },
  "commands": {
    "setup":             { "tier": "reasoning",    "agent": "setup-assistant",     "description": "Onboard a project into dev-team-agents (first run or refresh).",                          "plan_gate": "required",    "interaction_mode": "required" },
    "plan":              { "tier": "reasoning",    "agent": "product-analyst",     "description": "Plan a feature — product-analyst leads, optional software-architect.",                        "plan_gate": "required",    "interaction_mode": "required" },
    "architect":         { "tier": "reasoning",    "agent": "software-architect",  "description": "Architecture decisions, ADRs, trade-offs.",                                               "plan_gate": "required",    "interaction_mode": "required" },
    "adr":               { "tier": "reasoning",    "agent": "software-architect",  "description": "Create a new Architecture Decision Record.",                                            "plan_gate": "required",    "interaction_mode": "required" },
    "refactor":          { "tier": "reasoning",    "agent": "software-architect",  "description": "Structured refactoring: dependency mapping, ordered commits.",                        "plan_gate": "required",    "interaction_mode": "required" },
    "security":          { "tier": "reasoning",    "agent": "security-specialist",  "description": "Security audit or vulnerability analysis.",                                          "plan_gate": "required",    "interaction_mode": "required" },
    "backend":           { "tier": "backend-exec", "agent": "backend-developer",   "description": "Implement backend changes.",                                                        "plan_gate": "conditional", "interaction_mode": "optional" },
    "frontend":          { "tier": "frontend",     "agent": "frontend-developer",  "description": "Implement frontend changes.",                                                       "plan_gate": "conditional", "interaction_mode": "optional" },
    "fullstack":         { "tier": "backend-exec", "agent": "backend-developer",   "description": "Implement full-stack changes end-to-end.",                                            "plan_gate": "conditional", "interaction_mode": "optional" },
    "mobile":            { "tier": "backend-exec", "agent": "mobile-developer",    "description": "Implement mobile features.",                                                         "plan_gate": "conditional", "interaction_mode": "optional" },
    "fix":               { "tier": "backend-exec", "agent": "backend-developer",   "description": "Fix a bug (auto-routes to backend/frontend/mobile developer).",                      "plan_gate": "conditional", "interaction_mode": "optional" },
    "dba":               { "tier": "backend-exec", "agent": "database-specialist",  "description": "Schema design, query optimization, migrations.",                                    "plan_gate": "conditional", "interaction_mode": "optional" },
    "devops":            { "tier": "backend-exec", "agent": "devops-specialist",   "description": "CI/CD, Docker, infra, deploy scripts.",                                              "plan_gate": "conditional", "interaction_mode": "optional" },
    "review":            { "tier": "backend-exec", "agent": "code-reviewer",       "description": "Code review before merge.",                                                        "plan_gate": "conditional", "interaction_mode": "optional" },
    "qa":                { "tier": "backend-exec", "agent": "qa-specialist",       "description": "Validate feature behavior and acceptance criteria.",                                 "plan_gate": "conditional", "interaction_mode": "optional" },
    "design":            { "tier": "frontend",     "agent": "ui-ux-designer",      "description": "Design system, UX flows, visual decisions.",                                        "plan_gate": "conditional", "interaction_mode": "optional" },
    "relayout":          { "tier": "frontend",     "agent": "ui-ux-designer",      "description": "Redesign an existing screen to match visual references.",                            "plan_gate": "conditional", "interaction_mode": "optional" },
    "seo":               { "tier": "frontend",     "agent": "seo-specialist",      "description": "SEO quality gate — technical, on-page, Core Web Vitals, structured data, GEO/LLM readiness.", "plan_gate": "conditional", "interaction_mode": "optional" },
    "tester":            { "tier": "backend-exec", "agent": "backend-test-specialist", "description": "Write or update tests only.",                                                   "plan_gate": "conditional", "interaction_mode": "optional" },
    "docs":              { "tier": "repetitive",  "agent": "technical-writer",    "description": "Docs, changelogs, runbooks, release notes.",                                          "plan_gate": "conditional", "interaction_mode": "optional" },
    "pr":                { "tier": "repetitive",  "agent": "technical-writer",    "description": "Draft and create a pull request.",                                                  "plan_gate": "conditional", "interaction_mode": "optional" },
    "push":              { "tier": "repetitive",  "agent": "technical-writer",    "description": "Push, asking a CI/CD-aware quiz when GitHub Actions is configured.",                "plan_gate": "opt_out",     "interaction_mode": "optional" },
    "commit":            { "tier": "repetitive",  "agent": "technical-writer",    "description": "Group staged changes by layer and write commits.",                                  "plan_gate": "conditional", "interaction_mode": "optional" },
    "learn":             { "tier": "repetitive",  "agent": "technical-writer",    "description": "Consolidate session decisions into docs, wiki, ADRs.",                              "plan_gate": "conditional", "interaction_mode": "optional" },
    "explain":           { "tier": "repetitive",  "agent": "technical-writer",    "description": "Explain a term, acronym or jargon from the session, then quiz on it.",              "plan_gate": "conditional", "interaction_mode": "optional" },
    "update":            { "tier": "repetitive",  "agent": "technical-writer",    "description": "Check for and apply dev-team-agents updates.",                                      "plan_gate": "opt_out",     "interaction_mode": "optional" },
    "symlinks":          { "tier": "repetitive",  "agent": "technical-writer",    "description": "Diagnose and repair broken dev-team-agents symlinks.",                              "plan_gate": "opt_out",     "interaction_mode": "optional" },
    "health-check":      { "tier": "repetitive",  "agent": "technical-writer",    "description": "Health check — validate and repair dev-team-agents installation.",                  "plan_gate": "opt_out",     "interaction_mode": "optional" },
    "install":           { "tier": "repetitive",  "agent": "technical-writer",    "description": "Install complementary tools from a closed allowlist (rg, fd, jq, ast-grep, tokei, delta, graphify).", "plan_gate": "opt_out", "interaction_mode": "optional" },
    "audit":             { "tier": "backend-exec", "agent": "code-reviewer",     "description": "Deep analysis of a module/area — silent bugs, test gaps, edge cases, security, infra, and improvement plan.", "plan_gate": "conditional", "interaction_mode": "optional" },
    "rule":              { "tier": "repetitive",  "agent": "technical-writer",    "description": "Catalog a mandatory reuse/standardization rule into docs/development/reuse-guidelines.md.", "plan_gate": "conditional", "interaction_mode": "optional" },
    "sync-rules":        { "tier": "repetitive",  "agent": "technical-writer",    "description": "Scan docs/ for undocumented conventions and catalog them via /devteam:rule.", "plan_gate": "opt_out", "interaction_mode": "optional" }
  }
}
